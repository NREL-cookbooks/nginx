server {
  listen <%= node[:nginx][:listen] %> default_server;
  server_name  default.<%= node[:hostname] %>;

  access_log  <%= node[:nginx][:log_dir] %>/localhost.access.log;

  location / {
    root   /var/www/nginx-default;
    index  index.html index.htm;
  }
}
